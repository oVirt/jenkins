- project:
    name: infra-docs
    project:
      - infra-docs
    version:
      - master:
          branch: master
    stage:
      - check-patch
      - check-merged
    distro: fc21
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'
