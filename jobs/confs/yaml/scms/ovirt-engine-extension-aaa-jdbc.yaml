- scm:
    name: ovirt-engine-extension-aaa-jdbc-gerrit
    scm:
      - git:
          url: git://gerrit.ovirt.org/ovirt-engine-extension-aaa-jdbc.git
          branches:
            - $GERRIT_BRANCH
          basedir: ovirt-engine-extension-aaa-jdbc
          scm-name: ovirt-engine-extension-aaa-jdbc
          name: ''
          refspec: $GERRIT_REFSPEC
          choosing-strategy: gerrit
          use-author: true
          skip-tag: true
          prune: true
          wipe-workspace: false
