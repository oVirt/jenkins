- scm:
    name: ovirt-engine-extension-aaa-ldap-gerrit
    scm:
      - git:
          url: git://gerrit.ovirt.org/ovirt-engine-extension-aaa-ldap.git
          branches:
            - $GERRIT_BRANCH
          basedir: ovirt-engine-extension-aaa-ldap
          scm-name: ovirt-engine-extension-aaa-ldap
          name: ''
          refspec: $GERRIT_REFSPEC
          choosing-strategy: gerrit
          use-author: true
          skip-tag: true
          prune: true
          wipe-workspace: false
