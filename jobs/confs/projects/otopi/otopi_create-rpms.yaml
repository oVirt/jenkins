- project:
    name: otopi_create-rpms_created
    project:
      - otopi
    version:
      - 3.6:
          branch: otopi-1.4
    mock-build-type: all
    distro:
      - el6
      - el7
    arch: x86_64
    extra-build-packages: |
      gettext-devel
      automake
      autoreconf
      make
    jobs:
      - '{project}_{version}_create-rpms-{distro}-{arch}_created'

- project:
    name: otopi_create-rpms_merged
    project:
      - otopi
    version:
      - 3.6:
          branch: otopi-1.4
          repoversion: 3.6
    mock-build-type: all
    distro:
      - el6
      - el7
    arch: x86_64
    extra-build-packages: |
      gettext-devel
      automake
      autoreconf
      make
    jobs:
      - '{project}_{version}_create-rpms-{distro}-{arch}_merged'
