- project:
    name: opstools-ansible_create-rpms_created
    project: releng-tools
    subproject:
      - opstools-ansible
    version:
      - master:
          branch: master
    mock-build-type: all
    distro:
      - el7
      - fc24
      - fc25
    arch:
      - x86_64
    jobs:
      - '{subproject}_{version}_create-rpms-{distro}-{arch}_created':
          extra-build-packages: 'rpmdevtools'

- project:
    name: opstools-ansible_create-rpms_merged
    project: releng-tools
    subproject:
      - opstools-ansible
    version:
      - master:
          branch: master
          repoversion: master
    mock-build-type: all
    distro:
      - el7
      - fc24
      - fc25
    arch:
      - x86_64
    jobs:
      - '{subproject}_{version}_create-rpms-{distro}-{arch}_merged':
          extra-build-packages: 'rpmdevtools'
