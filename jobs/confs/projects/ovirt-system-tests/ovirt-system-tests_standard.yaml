- project:
    name: ovirt-system-tests_check-patch
    node-filter: integ-tests
    project:
      - ovirt-system-tests
    version:
      - master:
          branch: master
    stage:
      - check-patch
    distro:
      - fc23
    trigger: 'on-change'
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'
