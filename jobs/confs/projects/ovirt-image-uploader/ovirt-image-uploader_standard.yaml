- project:
    name: ovirt-image-uploader_master
    project:
      - ovirt-image-uploader
    version:
      - master:
          branch: master
    stage:
      - check-patch
      - check-merged
      - build-artifacts
    distro:
      - el7
      - fc23
    trigger: 'on-change'
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'

- project:
    name: ovirt-image-uploader_3.6
    project:
      - ovirt-image-uploader
    version:
      - 3.6:
          branch: ovirt-image-uploader-3.6
    stage:
      - check-patch
      - build-artifacts
    distro:
      - el6
      - el7
      - fc22
      - fc23
    trigger: 'on-change'
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'

- project:
    name: ovirt-image-uploader_3.5
    project:
      - ovirt-image-uploader
    version:
      - 3.5:
          branch: ovirt-image-uploader-3.5
    stage:
      - check-patch
      - build-artifacts
    distro:
      - el6
      - el7
    trigger: 'on-change'
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'
