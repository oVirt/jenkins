- project:
    name: ovirt-engine-api-metamodel_build
    project: ovirt-engine-api-metamodel
    version:
      - master:
          branch: master
    stage: build-artifacts
    trigger: 'on-change'
    distro:
      - el7
    arch: x86_64
    jobs:
      - 'ovirt-engine-api-metamodel_{version}_build-artifacts-{distro}-{arch}'
