- project:
    name: ovirt-engine-nodejs-modules_create-rpms
    project: releng-tools
    subproject:
      - ovirt-engine-nodejs-modules
    trigger:
      - created
      - merged
    version:
      - master:
          branch: master
    mock-build-type: all
    distro:
      - el7
      - fc23
    arch: x86_64
    jobs:
      - '{subproject}_{version}_create-rpms-{distro}-{arch}_{trigger}':
          extra-build-packages: |
            gcc-c++
            ovirt-engine-nodejs
            rpmdevtools
            wget
