- project:
    name: qemu_create-rpms
    project: releng-tools
    subproject:
      - qemu
    trigger:
      - created
      - merged
    version:
      - master:
          branch: master
    mock-build-type: all
    distro:
      - el7:
          extra-repos: |
              glusterfs-centos,http://mirror.centos.org/centos/7/storage/\\$basearch/gluster-3.7/
    arch: x86_64
    jobs:
      - '{subproject}_{version}_create-rpms-{distro}-{arch}_{trigger}':
          extra-build-packages: 'wget'
