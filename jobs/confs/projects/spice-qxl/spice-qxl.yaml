- project:
    name: spice-qxl
    project: ovirt-wgt-toolchain
    subproject:
      - spice-qxl
    trigger:
      - created
      - merged
    version:
      - 3.5:
          branch: ovirt-wgt-toolchain-3.5
    mock-build-type: all
    distro: fc23
    arch: x86_64
    extra-build-packages: |
      rpmdevtools
    jobs:
      - '{subproject}_{version}_create-rpms-{distro}-{arch}_{trigger}'
