- project:
    name: ovirt-imageio
    project:
        - ovirt-imageio
    version:
        - master:
            branch: master
    stage:
        - check-patch
        - check-merged
    distro:
        - fc23
        - el7
    trigger: 'on-change'
    arch:
        - x86_64
    jobs:
        - '{project}_{version}_{stage}-{distro}-{arch}'
