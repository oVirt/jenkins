- project:
    name: ovirt-engine-api-model_checks
    project:
      - ovirt-engine-api-model
    version:
      - master:
          branch: master
    stage: check-patch
    trigger: 'on-change'
    distro:
      - el7
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'
