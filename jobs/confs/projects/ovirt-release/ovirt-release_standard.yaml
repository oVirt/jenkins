- project:
    name: ovirt-release
    project: ovirt-release
    version:
      - master:
          branch: master
      - 4.0:
          branch: ovirt-4.0
      - 3.6:
          branch: ovirt-3.6
    stage:
      - check-patch:
          trigger: 'on-change'
      - build-artifacts:
          trigger: 'timed'
          trigger-times: '@midnight'
    distro:
      - el7
    arch: x86_64
    jobs:
      - '{project}_{version}_{stage}-{distro}-{arch}'
