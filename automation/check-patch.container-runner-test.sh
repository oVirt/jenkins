#!/bin/bash
# check-patch.container-runner-test.sh - This script exists so that the
# `container-runner-test.sh` substage would be enabled from STDCI`s POV.
# It will not actually get executed currently.
#
echo "This code should not be running"
exit 1
